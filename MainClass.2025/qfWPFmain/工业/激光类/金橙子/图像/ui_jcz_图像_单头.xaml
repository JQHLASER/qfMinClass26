<UserControl x:Class="qfWPFmain.ui_jcz_图像_单头"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:qfWPFmain"
             mc:Ignorable="d" 
             BorderBrush="Silver"  MouseDoubleClick="UserControl_MouseDoubleClick" SizeChanged="UserControl_SizeChanged" 
             Background="White">

    <!--<UserControl.Resources >-->


        <!--<Label   x:Key="_背景"  x:Shared="False"  
                Content ="{Binding Text_未初始化提示}" 
                VerticalAlignment="Stretch"     HorizontalAlignment="Stretch"   
                VerticalContentAlignment="Center"  HorizontalContentAlignment="Center"  
                FontSize="15" Foreground="White" FontFamily="微软雅黑"
                Background="Gray" Visibility="Visible"  />

        <Border x:Key="_图像" x:Shared="False" Background="White" Margin="0" BorderThickness="0">
            <Image   Visibility="Visible" Source="{Binding Img}" />
        </Border>
    </UserControl.Resources>-->

    <Grid x:Name="_grid" Margin="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >

        

    </Grid>
    
    
    <!--<Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="0"/>
            <RowDefinition Height="{Binding Height_未初始化提示}"/>
            <RowDefinition Height="{Binding Height_image}"/>
        </Grid.RowDefinitions>

        --><!--<ContentControl Content="{StaticResource _背景 }"  Grid.Row="0"/>--><!--
        <ContentControl Content="{StaticResource _背景 }"  Grid.Row="1"/>
        <ContentControl Content="{StaticResource _图像 }" Grid.Row="2"   />

    </Grid>-->
</UserControl>
