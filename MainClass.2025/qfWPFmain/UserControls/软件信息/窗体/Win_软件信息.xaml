<Window x:Class="qfWPFmain.Win_软件信息"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:qfWPFmain" 
        xmlns:qfwpfmain="clr-namespace:qfWPFmain"
        mc:Ignorable="d"
        Title="" Height="500" Width="700" WindowStyle="None"   
        Loaded="Window_Loaded"   >
    
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <!--标题栏-->
        <qfwpfmain:ui_window_Title Grid.Row="0" x:Name="_标题栏"   ui_is普通标题栏="True" 
                                   BorderBrush="Silver" BorderThickness="0,0,0,1.5"
                                   ui_IsMinButton="False" />

      
            <!--信息区-->
        <TextBox Grid.Row="1" x:Name="_textBox_信息" Margin="10" IsReadOnly="true" TextWrapping="NoWrap"
                 Text="{Binding Info_信息}" BorderThickness="0" 
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto" 
                 FontSize=" 14" FontFamily=" 微软雅黑"/>


        <!--按钮栏-->
        <Border Grid.Row="2" BorderBrush="Silver" BorderThickness="0,1,0,0">
            <qfwpfmain:ui_ButtonBorder x:Name="_button_Ok"  Height="50" Width="200" ui_Text="OK" PreviewMouseLeftButtonUp="_button_Ok_PreviewMouseLeftButtonUp"/>
        </Border>

    </Grid>
</Window>
